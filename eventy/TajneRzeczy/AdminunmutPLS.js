module.exports = (client, przed, po) => {
    // if (przed.channel.name === null || po.channel.name === null) return;
    // if (po.channel.name === `${przed.channel.name}`) return;
    // if (po.channel.name.startsWith(`Sesja`)) {
    //     po.member.voice.setMute(mute = false, `Ok`)
    //     console.log(`unmut`)
    // } else
    // return;
};